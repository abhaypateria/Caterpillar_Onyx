import Stub from './Stub';

export default function Supervisor() {
  return <Stub title='Supervisor' owner='B' items={['Fleet status', 'Incident list + replay timeline', 'Dispatcher nudges from root cause', 'Sync incidents to /api/incidents']} />;
}
