import Stub from './Stub';

export default function Insights() {
  return <Stub title='Insights' owner='A' items={['analyse() over telemetry: findings table', 'Idle chart vs 25% target, idle cost ₹ / L / CO₂', 'Fuel mismatch flags for review', 'Operator profile (safety, productivity, fuel)']} />;
}
