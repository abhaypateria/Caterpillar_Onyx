import Stub from './Stub';

export default function Dashboard() {
  return <Stub title="Today" owner="A" items={['Current task card with Bayesian ETA + range + why', 'Start / +1 cycle / Done → live ETA', "Today's list with status", 'Planner vs Onyx on the 5 provided tasks']} />;
}
