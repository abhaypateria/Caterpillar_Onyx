import Stub from './Stub';

export default function Safety() {
  return <Stub title='Safety' owner='A' items={['Engine + seatbelt toggles → seatbeltStage escalation + auto incident', 'Proximity: webcam COCO-SSD + simulated radar, distances from proximityThresholds()', 'Conditions from Open-Meteo, WBGT + break timer', 'Incident form + list']} />;
}
