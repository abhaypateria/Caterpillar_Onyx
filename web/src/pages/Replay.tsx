import Stub from './Stub';

export default function Replay() {
  return <Stub title='Replay' owner='A' items={['Play DEMO_DAY windows through hmm.step()', 'Risk gauge + state probabilities + root cause + nudge (spoken)', 'Reveal: warned at 09:10, belt off 09:30', 'Backtest table: catch rate, lead time, false alarms, control day']} />;
}
