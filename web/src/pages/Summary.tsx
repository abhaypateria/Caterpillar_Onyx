import Stub from './Stub';

export default function Summary() {
  return <Stub title='Shift summary' owner='B' items={['Hours, tasks, idle + cost, alerts, lessons', 'Spoken summary via api.summary() with template fallback', 'WhatsApp owner summary (wa.me link)']} />;
}
