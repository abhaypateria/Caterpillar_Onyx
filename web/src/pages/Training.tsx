import Stub from './Stub';

export default function Training() {
  return <Stub title='Training' owner='B' items={['Lesson list + player (audio via speak()) + quiz', 'Recommended from findings / risk', 'Near-miss → lesson via api.lessonFromIncident()', 'Instructor booking']} />;
}
