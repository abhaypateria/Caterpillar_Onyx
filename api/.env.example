# Copy to api/.env (never commit .env). Everything works without keys via template fallbacks.
# LLM: Groq models tried in order (free tier: 1000 requests/day and 8000 tokens/min per model).
GROQ_API_KEY=
# GROQ_MODELS=openai/gpt-oss-120b,openai/gpt-oss-20b,qwen/qwen3.8-27b
SARVAM_API_KEY=
